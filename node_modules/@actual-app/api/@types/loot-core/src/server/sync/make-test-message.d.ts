export declare function makeTestMessage(keyId: any): Promise<{
    value: NonSharedBuffer;
    meta: {
        keyId: any;
        algorithm: "aes-256-gcm";
        iv: string;
        authTag: string;
    };
}>;
