export declare function randomBytes(n: any): NonSharedBuffer;
export declare function encrypt(masterKey: any, value: any): {
    value: NonSharedBuffer;
    meta: {
        keyId: any;
        algorithm: "aes-256-gcm";
        iv: string;
        authTag: string;
    };
};
export declare function decrypt(masterKey: any, encrypted: any, meta: any): NonSharedBuffer;
export declare function createKey({ secret, salt }: {
    secret: any;
    salt: any;
}): {
    raw: NonSharedBuffer;
    base64: string;
};
export declare function importKey(str: any): {
    raw: Buffer<ArrayBuffer>;
    base64: any;
};
